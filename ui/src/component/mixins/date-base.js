import props from '../utils/props'

export default {
  name: 'DateBase',

  props: {
    ...props.common,
    ...props.base,
    ...props.locale
  },

  data () {
    return {
      //
    }
  },

  computed: {
    //
  },

  methods: {
    //
  }
}
